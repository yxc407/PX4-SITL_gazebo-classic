syntax = "proto2";
package sensor_msgs.msgs;
import "vector3d.proto";

message Position {
  required gazebo.msgs.Vector3d xyz = 1;
  required gazebo.msgs.Vector3d lla = 2;
  required int64 time_usec = 3;
  required int64 seq = 4;
}